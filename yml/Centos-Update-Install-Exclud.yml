- name: "Mise a jours avec restriction docker"
  hosts: linux
  remote_user: root
  
  tasks:
  - name: "Mise a jours avec restriction docker"
    yum:
      name: '*'
      state: latest
    exclude: docker*